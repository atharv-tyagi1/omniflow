# docker-compose.override.yml
# This file previously defined an incomplete 'backend' service that caused
# Docker Compose merge errors. That service definition has been removed.
# The primary docker-compose.yml contains the full postgres service definition.
services: {}
